apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: com-clean-dev
spec:
  type: ClusterIP
  selector:
    app: mariadb
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
